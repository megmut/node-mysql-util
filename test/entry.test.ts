import * as should from 'should'
// import * as Sinon from 'sinon';


describe('Accounts Controller Test', () => {

    it('Run a basic test successfully', async () => {
        should(0).be.exactly(0);
    });
});